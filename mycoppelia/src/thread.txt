

	non Threaded child script :
・各シミュレーションステップで発生する関数呼び出し、長いループまたは無限ループがある場合、シミュレーションはブロックし、ループが終了するまで待機してから再開します。
＊スクリプトが基本的に中断されることはない。

	Threaded child script :
・シミュレーションの開始時に単一の呼び出しが行われます。長いループ股が無限ループがある場合でも、シミュレーションは続行されます。
＊基本的にスクリプトが実行中の各シミュレーションステップで中断され、次のシミュレーションステップで再開される。
